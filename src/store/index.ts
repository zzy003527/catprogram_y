import { createStore } from 'vuex'

export default createStore({
  state: {
    // 控制登录卡片的变量
    dialogTableVisible: true,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
